package solvers;

import searchable.matrix.Position;
import searcher.Searcher;

public class MatrixSolver extends CommonSolver<Position> {

    public MatrixSolver(Searcher<Position> searcher) {
        super(searcher);
    }
}
